[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# دعم تطبيق الصفحة الواحدة (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
